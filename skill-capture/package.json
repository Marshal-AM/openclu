{
  "name": "skill-capture",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test:e2e-training": "node cli/node_modules/tsx/dist/cli.mjs scripts/e2e-training-publish-purchase.ts",
    "orchestrator": "npm run start --prefix orchestrator",
    "setup": "npm run setup:node && npm run setup:python",
    "setup:python": "python scripts/install-python-deps.py",
    "setup:node": "npm install && npm install --prefix cdr && npm install --prefix arkiv && npm install --prefix cli && npm install --prefix orchestrator"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "qrcode-terminal": "^0.12.0",
    "viem": "^2.21.0"
  }
}
