# Supabase — ONLY place that talks to your database (Vercel + local dev)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Fallback orchestrator when session cookie has no ngrok URL (local all-in-one dev)


# Device: skill-capture/.env has GROQ_API_KEY, NGROK_AUTHTOKEN, FRONTEND_URL (for register.sh → this app)
# Orchestrator on device has NO Supabase config.
